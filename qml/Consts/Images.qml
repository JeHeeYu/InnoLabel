import QtQuick 2.15

Item {
    id: root

    readonly property string folderOpen: Qt.resolvedUrl("../../images/folder_open.svg")
    readonly property string next: Qt.resolvedUrl("../../images/next.svg")
    readonly property string back: Qt.resolvedUrl("../../images/back.svg")
}
